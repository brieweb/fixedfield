package fixedfield.ast;

public class NineNode extends FixNode {

	public NineNode() {
		super();
	}
	
	@Override
	public String toString() {
		return "NineNode [  id="  + this.getId() + " start=" + this.getStart() + " count=" + this.getCount() + " ]";
	}
}
